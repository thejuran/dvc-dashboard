# DVC Dashboard Configuration
# Copy to .env and modify as needed: cp .env.example .env

# Server host address (default: 0.0.0.0)
# Set to 127.0.0.1 to restrict to local connections only
# HOST=0.0.0.0

# Server port (default: 8000)
# PORT=8000

# Database URL (default: SQLite in data/db/ directory)
# Uses async SQLite driver; only change if you know what you're doing
# DATABASE_URL=sqlite+aiosqlite:///./data/db/dvc.db

# CORS allowed origins (comma-separated, default: http://localhost:5173)
# In Docker, the frontend is served by FastAPI so CORS is less relevant
# CORS_ORIGINS=http://localhost:5173,http://localhost:8000
